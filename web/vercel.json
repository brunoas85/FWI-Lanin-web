{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "echo 'Sin dependencias de Node — nada que instalar'",
  "buildCommand": "bash build.sh",
  "outputDirectory": "public",
  "rewrites": [
    { "source": "/api/proxy/(.*)", "destination": "/api/proxy" }
  ]
}
